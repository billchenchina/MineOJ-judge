cmake_minimum_required(VERSION 3.2)

PROJECT(MineOJ-Judge)

INCLUDE_DIRECTORIES(
include,
gtest_src
)

AUX_SOURCE_DIRECTORY(src DIR_SRCS)

SET(TEST_MATH
${DIR_SRCS}
)

ADD_EXECUTABLE(${PROJECT_NAME} ${TEST_MATH})

TARGET_LINK_LIBRARIES(${PROJECT_NAME} m)
